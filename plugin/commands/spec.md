---
name: spec
description: Produce spec.md from an accepted intent.md, applying the organisation's policy skills
argument-hint: <change-slug>
---
Use the spec-format skill on `intent/$ARGUMENTS/intent.md`. Apply every
policy skill available so the spec conforms to brand, security, compliance
and UX standards. Write `intent/$ARGUMENTS/spec.md` and lead your summary
with the areas of concern, especially where policies contradict.
