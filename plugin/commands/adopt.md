---
name: adopt
description: Brownfield-adopt the AI-native SDLC in this repository, one play at a time
argument-hint: [assess | claude-md | intent | hooks | review | all]
---
Use the adopt skill on this repository. Scope: `$ARGUMENTS` (default:
`assess`, which changes nothing). Show a diff before touching any existing
file, and finish with the adoption report.
